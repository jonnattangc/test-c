# eth_names

Lista las interfaces de red del sistema (usa `getifaddrs`).

## Compilar

```sh
g++ eth_names.cpp -o eth_names -Wall -Wextra -Wpedantic -Werror -std=c++17
```

## Ejecutar

```sh
./eth_names
```
