# servidor_t

Variante de servidor TCP de un laboratorio de sockets (estilo K&R, pre-C99).

## Compilar

Código legado; compila con advertencias pero no con los flags estrictos del proyecto:

```sh
gcc servidor_t.c -o servidor_t -Wall -Wextra
```

## Ejecutar

```sh
./servidor_t
```
